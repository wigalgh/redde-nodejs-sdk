{
  "name": "redde",
  "version": "1.0.0",
  "description": "Redde allows Merchants to receive, send, check transaction status, and perform lots of payment transactions",
  "main": "redde.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Ricardo Volvox",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.19.0",
    "express": "^4.17.1",
    "request": "^2.88.0"
  }
}
